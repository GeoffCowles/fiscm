allow function specification through namelists

add input control
add interpolation of scalar fields from input
add advection/diffusion

add new routine to open the netcdf output files so that init_bio can dump
global vars to it.

geoff's change


ji add 

